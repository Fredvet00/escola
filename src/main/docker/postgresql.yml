version: '2'
services:
  escola-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/escola/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=escola
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
